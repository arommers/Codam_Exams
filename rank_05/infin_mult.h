#ifndef INFIN_MULT_H
# define INFIN_MULT_H

#include <stdlib.h>
#include <unistd.h>
#include <stddef.h>

size_t  ft_strlen(const char* str);
void    multiply_strings(const char* num1, const char* num2);

#endif